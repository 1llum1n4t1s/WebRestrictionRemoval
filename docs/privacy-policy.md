# プライバシーポリシー - WEB制限解除サポート

最終更新日: 2026年4月18日

## はじめに

「WEB制限解除サポート」（以下「本拡張機能」）は、ユーザーのプライバシーを尊重し、個人情報の保護に努めます。本プライバシーポリシーは、本拡張機能におけるデータの取り扱いについて説明します。

## 収集するデータ

本拡張機能は、個人情報を一切収集しません。

## ローカルに保存するデータ

本拡張機能は、以下の設定データをユーザーの端末内（`chrome.storage.local`）にのみ保存します。

- **拡張機能の有効/無効（単一トグル）**: `enabled` キー（真偽値）

この値は、サイレント自動解除（右クリック制限・テキスト選択制限）の有効化状態、および右クリックメニュー（強制ペースト・強制コピー）の表示有無を制御するためのみに使用されます。端末内にのみ保存され、外部サーバーへの送信は一切行いません。

## クリップボードへのアクセス

右クリックメニューから「強制ペースト」または「強制コピー」を実行したときにのみ、当該操作の完了に必要な範囲でクリップボードにアクセスします。クリップボードの内容が外部に送信されたり、拡張機能内部に保存されたりすることはありません。

## データの共有

本拡張機能は、いかなるデータも第三者と共有しません。

## ネットワーク通信

本拡張機能は、外部サーバーとの通信を一切行いません。すべての処理はユーザーの端末内で完結します。

## 権限の使用目的

- **activeTab**: ユーザーがポップアップから有効/無効を切り替えた際、現在のタブに反映するために使用します。
- **scripting**: メインワールドでインラインハンドラを除去するためのスクリプトを現在のタブに注入する目的で使用します。
- **storage**: 拡張機能の有効/無効設定（`enabled`）をローカルに保存・復元するために使用します。
- **contextMenus**: 右クリックメニューに「強制ペースト」「強制コピー」を表示するために使用します。
- **clipboardRead**: 「強制ペースト」実行時にクリップボードの内容を読み取るために使用します。
- **clipboardWrite**: 「強制コピー」実行時に選択テキストをクリップボードへ書き込むために使用します。
- **offscreen**: HTTPページ上でも安全にクリップボードへアクセスするために、オフスクリーン ドキュメント（extension コンテキスト）を利用します。

## お問い合わせ

本プライバシーポリシーに関するご質問は、Chrome Web Storeのサポートページよりお問い合わせください。

## 変更について

本プライバシーポリシーは予告なく変更される場合があります。変更があった場合は、本ページを更新します。
