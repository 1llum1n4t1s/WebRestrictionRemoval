{
  "name": "web-viewing-assist",
  "version": "1.0.43",
  "type": "commonjs",
  "description": "Chrome extension that bundles the YouTube cleaner, Amazon auto-delivery totals, and a per-tab volume booster.",
  "scripts": {
    "ci:install": "pnpm install --frozen-lockfile",
    "generate-icons": "node scripts/generate-icons.js",
    "generate-screenshots": "node webstore/generate-screenshots.js",
    "build": "pnpm run generate-icons && pnpm run generate-screenshots",
    "test": "node --test",
    "lint": "eslint src/ test/ scripts/"
  },
  "devDependencies": {
    "chrome-webstore-upload-cli": "^4.0.0",
    "eslint": "^10.5.0",
    "globals": "^17.6.0",
    "puppeteer": "^25.0.4",
    "sharp": "0.35.1",
    "web-ext": "^10.4.0"
  },
  "keywords": [
    "chrome-extension",
    "youtube-cleaner",
    "amazon",
    "volume-booster",
    "web-viewing"
  ],
  "author": "",
  "license": "MIT"
}
